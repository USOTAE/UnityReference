using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//todo 不再做stats的tooltip展示
public class UI_StatsToolTip : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
